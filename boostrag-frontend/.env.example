# Backend API base URL (defaults to http://127.0.0.1:8000 when unset)
# VITE_BOOSTRAG_API_URL=https://boostrag.onrender.com

# --- Accounts (Supabase) — frontend; absent locally = anonymous-only ---
# Both are safe to expose in the browser (anon key is protected by RLS).
# VITE_SUPABASE_URL=https://<project>.supabase.co
# VITE_SUPABASE_ANON_KEY=...
